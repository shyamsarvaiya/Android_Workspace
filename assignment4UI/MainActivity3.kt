package com.example.uiassignment4

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.EditText
import android.widget.RadioButton
import android.widget.TextView

//program - 3 Create an application with radio buttons (Add, Subtraction, Multiply,
//Division) EditText (number1, number2) and print result as per user choice
//from radio button in TextView
class MainActivity3 : AppCompatActivity() {
 /*   lateinit var ed1:EditText
    lateinit var ed2:EditText
    lateinit var rbAdd:RadioButton
    lateinit var rbSubtract:RadioButton
    lateinit var rbMultiply:RadioButton
    lateinit var rbDivision:RadioButton
    lateinit var result:TextView

 */
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main3)

        /*ed1 = findViewById(R.id.ed1)
        ed2 = findViewById(R.id.ed2)
        rbAdd = findViewById(R.id.rbAdd)
        rbSubtract = findViewById(R.id.rbSubtract)
        rbMultiply = findViewById(R.id.rbMultiply)
        rbDivision = findViewById(R.id.rbDivision)
        result = findViewById(R.id.result)

        rbAdd.setOnClickListener()

         */

    }
}